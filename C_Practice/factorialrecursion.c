/*
#include<stdio.h>
#include<string.h>

int factorialrecursion(int a)
{
  if(a==0 || a==1)
    return 1;
  else
   return a*factorialrecursion(a-1);
}

int main()
{
  int a;
  printf("Enter the number : ");
  scanf("%d",&a);
  int fact=factorialrecursion(a);
  printf("The factorial of %d id %d",a,fact);  
}
*/