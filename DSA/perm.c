#include<stdio.h>
/*
void perm(char arr[],int n,int ix,char p[],int psize)
{
  if(psize==n)
    printf("%s",p);
  p[psize++]=arr[ix];
  perm(arr,n,ix+1,p,psize+1);
  perm(arr,n,ix+1,p,psize);
}

int main()
{
  char arr[]={"ABC"};
  int n=3;
  int p[100];
  perm(arr,n,0,p,p,0);
  return 0;
}
  */