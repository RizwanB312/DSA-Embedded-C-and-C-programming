#include<stdio.h>
/*
void permutation(char arr[],int l,int r)
{
 int temp;
 if(l==r)
   printf("%s\n",arr);
for(int i=l;i<=r;i++)
  {
    temp=arr[i];
    arr[i]=arr[l];
    arr[l]=temp;
    permutation(arr,l+1,r);
      temp=arr[i];
      arr[i]=arr[l];
      arr[l]=temp;
  }
}

int main()
{
  char arr[]={"ABC"};
  int n=3;
  int l=0;
  int r=n-1;
  permutation(arr,l,r);
  return 0;
}*/