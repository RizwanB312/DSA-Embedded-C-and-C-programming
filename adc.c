/*
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// Mock ADC function
int readADC() {
    return rand() % 1024;  // Simulate 10-bit ADC value
}

int main() {
    srand(time(0));  // Seed random number

    for (int i = 0; i < 5; i++) {
        int value = readADC();
        printf("ADC Reading %d: %d\n", i + 1, value);
    }

    return 0;
}
*/
